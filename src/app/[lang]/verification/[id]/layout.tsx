import { Wrapper } from "@/processes/VerificationProcess";
import React from "react";

const VerificationLayout = ({ children }: { children: React.ReactNode }) => {
  return <Wrapper>{children}</Wrapper>;
};

export default VerificationLayout;
